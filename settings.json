{
  "settings": [
    {
      "key": "homeLayout",
      "label": "Home Layout",
      "description": "How platforms are displayed on the home screen",
      "type": "select",
      "options": [
        { "value": "default", "label": "Default" },
        { "value": "grid", "label": "Grid" }
      ],
      "default": "default"
    },
    {
      "key": "gamesLayout",
      "label": "Games Layout",
      "description": "How games list is displayed",
      "type": "select",
      "options": [
        { "value": "default", "label": "Grid" },
        { "value": "carousel", "label": "Carousel" }
      ],
      "default": "default"
    },
    {
      "key": "gameLayout",
      "label": "Game Detail Layout",
      "description": "How game details are displayed",
      "type": "select",
      "options": [{ "value": "default", "label": "Default" }],
      "default": "default"
    },
    {
      "key": "gameCardFormat",
      "label": "Game Card Format",
      "description": "How game cards are displayed in the games list",
      "type": "select",
      "options": [
        { "value": "cover", "label": "Cover" },
        { "value": "screenshot", "label": "Screenshot + Logo" }
      ],
      "default": "cover"
    },
    {
      "key": "showGameInfo",
      "label": "Show Game Info Panel",
      "description": "Show the game information panel on the right side of the games list",
      "type": "toggle",
      "default": true
    }
  ]
}
